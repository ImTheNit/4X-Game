-- Drop the Game table if it exists
DROP TABLE IF EXISTS Game;

-- Drop the Player table if it exists
DROP TABLE IF EXISTS Player;

-- Drop the database if it exists
DROP DATABASE IF EXISTS FourXGame;
